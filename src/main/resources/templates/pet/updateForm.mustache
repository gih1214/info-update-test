<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta http-equiv="X-UA-Compatible" content="IE=edge">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>반려동물 정보 수정 테스트</title>
    <!-- 제이쿼리 -->
    <script src="https://ajax.googleapis.com/ajax/libs/jquery/3.5.1/jquery.min.js"></script>
</head>

<body>
    <h1>반려동물 정보 수정 테스트</h1>
    <hr />

    <!-- 반려동물 정보 시작 -->
    <span><b>반려동물 정보</b></span>

    <input id="id" type="hidden" value="{{pet.user.id}}">
    <!--반려동물정보 수정 form 시작-->
    <form action="/s/user/{{pet.user.id}}/pet" method="post">
        <div>이름</div>
        <div>
            <input type="text" name="petName" placeholder="펫 이름" value="{{pet.petName}}" />
        </div>
        <div>성별</div>
        <div>
            <input type="text" name="petGender" placeholder="성별" value="{{pet.petGender}}" />
        </div>
        <div>종류</div>
        <div>
            <input type="text" name="petSpices" placeholder="반려동물 종" value="{{pet.petSpices}}" />
        </div>
        <div>나이</div>
        <div>
            <input type="text" name="petAge" placeholder="나이" value="{{pet.petAge}}" />
        </div>
        <br>
        <!-- 반려동물 정보 끝 -->

        <button type="submit"><a href="/s/api/user/{{pet.user.id}}/pet-update-form">수정하기</a></button>
        <button type="button" onclick="deletePet('{{pet.id}}');">삭제하기</button>

    </form>
    <!--반려동물정보 수정 form 끝-->
</body>

<script>
    async function deletePet(petId) {
        let response = await fetch(`/s/api/pet/${petId}`, {
            method: "DELETE"
        });
        let responseParse = await response.json();
        if (responseParse.code == 1) {
            alert("삭제 성공");
            location.reload(); // ajax + ssr
        } else {
            alert("삭제 실패");
        }
    }
</script>

</html>