<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta http-equiv="X-UA-Compatible" content="IE=edge">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>회원정보 수정 테스트</title>
    <!-- 제이쿼리 -->
    <script src="https://ajax.googleapis.com/ajax/libs/jquery/3.5.1/jquery.min.js"></script>
</head>

<body>
    <h1>회원정보 수정 테스트</h1>
    <hr />

    <input id="id" type="hidden" value="{{user.id}}">

    <!-- 회원사진 수정 시작 -->
    <form action="/s/user/{{user.id}}/img-upload" method="post" enctype="multipart/form-data">
        <!-- 이미지 지연 로딩 onerror 써서 해결 -->
        <img id="my-img" src="/upload/{{user.userImgurl}}" onerror="this.src='/upload/{{user.userImgurl}}'" width="300"
            height="300"><br>
        <input type="text" value="{{user.userImgurl}}">
        <input type="file" name="file"><br><br>
        <button type="submit">프로필 사진 수정하기</button><br><br>
    </form>
    <!-- 회원사진 수정 끝-->

    <script>
        $(document).ready(function () {
            setTimeout(() => {
                $("#my-img").attr("src", "/upload/{{user.userImgurl}}");
            }, 1000);
        });
    </script>

    <!--회원정보 수정 form 시작-->
    <form>
        <!--회원정보 수정-->
        <span><b>회원 정보</b></span>

        <div>아이디</div>
        <div>
            <input type="text" name="user-id" value="{{user.userId}}" readonly />
        </div>
        <div>닉네임</div>
        <div>
            <input id="user-name" type="text" name="userName" placeholder="유저네임" value="{{user.userName}}" />
        </div>
        <div>패스워드</div>
        <div>
            <input id="password" type="password" name="password" placeholder="패스워드" value="{{user.password}}"
                required="required" />
        </div>
        <div>전화번호</div>
        <div>
            <input id="phone-num" type="text" name="phoneNum" placeholder="휴대폰번호" value="{{user.phoneNum}}" />
        </div>
        <br>
        <button id="btn-update" type="button">수정하기</button>
    </form>
    <!--회원정보 수정 form 끝-->
    <hr />

    <!-- 반려동물 정보 -->
    <span><b>반려동물 정보</b></span>
    <br>
    <!--제출버튼-->
    <button type="button"><a href="/s/user/{{user.id}}/pet-form">바로가기</a></button>
    <button>삭제하기</button>
    <!--제출버튼 끝-->
</body>

<script src="/js/user.js"></script>
<script>

</script>

</html>